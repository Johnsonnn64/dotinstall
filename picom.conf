# shadow setting
shadow = false;
shadow-offset-x = -2;
shadow-offset-y = -2;
shadow-radius = 7;
shadow-opacity = 0.6;
shadow-exclude = [
  "class_g = 'dwm'",
  "class_g = 'Conky'",
  "class_g = 'Dunst'",
  "class_g = 'Cairo-clock'",
  "_GTK_FRAME_EXTENTS@:c",
  "class_g = 'dmenu'"
];

# fade setting
fading = false;
fade-in-step = 0.11;
fade-out-step = 0.11;
fade-exclude = [];

# opacity setting
opacity-rule = [ "100:class_g = 'st-256color'", "98:class_g = 'Alacritty'"];
frame-opacity = 1.0;
inactive-opacity-override = true;
active-opacity = 1.00;
inactive-opacity = 0.95; 
focus-exclude = [ "class_g = 'dwm'", "class_g = 'firefox'", "class_g = 'mpv'", "class_g = 'Chromium'", "class_g = 'qutebrowser'"];
blur:
{
  # method = "dual_kawase";
  strength = 3;
}

# others
backend = "glx";
vsync = false; # enable to remove screen tearing
mark-wmwin-focused = false;
mark-ovredir-focused = false;
detect-rounded-corners = true;
detect-client-opacity = true;
use-ewmh-active-win = true;
detect-transient = true;
detect-client-leader = true;
use-damage = true;
log-level = "warn";
corner-radius = 0;
round-borders = 0;
rounded-corners-exclude = ["class_g = 'Dunst'", "class_g = 'dwm'"];
experimental-backends = true;

# animations
animations = true;
# auto, none, fly-in, zoom, slide-down, slide-up, slide-right, slide-in, slide-out, zoom
animation-for-transient-window = "zoom";
animation-for-open-window = "zoom";
animation-for-unmap-window = "zoom";
animation-dampening = 25;

# exclusions
wintypes:
{
  tooltip = { fade = true; shadow = true; opacity = 0.75; focus = true; full-shadow = false; };
  dock = { shadow = false; };
  dnd = { shadow = false; };
  popup_menu = { fade = true; shadow = false; opacity = 1; };
  dropdown_menu = { fade = true; shadow = false; opacity = 1; };
};
